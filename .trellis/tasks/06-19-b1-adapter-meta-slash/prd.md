# B1 适配器meta扩展-slash分流字段

## Goal

plugin-adapter-meta.ts扩展字段: configPageCommands(进配置页的斜杠数组), slashBehavior Record<cmd,'notify'|'config-page'>。probe阶段标注每插件斜杠类型。依赖: tui-replacement-and-adapters.md §3.1,§6

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
