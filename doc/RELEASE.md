# Release 说明

各版本更新内容以仓库根目录 **[CHANGELOG.md](../CHANGELOG.md)** 为准。

GitHub Releases 正文仅保留摘要与下载说明，并链接到对应 tag 下的 `CHANGELOG.md`（例如 `v0.4.10` → `blob/v0.4.10/CHANGELOG.md`）。

发布维护：在 `CHANGELOG.md` 顶部追加新版本小节；打 tag 后由 CI `release` job 生成 Release 说明链接。